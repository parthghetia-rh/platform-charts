{{- if or .Values.gatekeeper.enabled 
          .Values.acs.enabled 
          (and .Values.authorino.enabled (not .Values.rhoai.enabled))
          .Values.rhtas.enabled
          .Values.keycloak.enabled 
}}
{{- $prefix := "" }}
{{- if eq .Values.controller.name "workshop" }}
  {{- $prefix = "workshop-" }}
{{- end }}
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: "{{ $prefix }}security"
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  goTemplate: true
  goTemplateOptions:
    - "missingkey=error"
  generators:
    - list:
        elements:
        {{- if ne .Values.controller.name "workshop" }}
          - application: "system-roles-service"
            namespace: system-roles
            chart: 
              name: system_roles
              url: https://na-launch-workshop.github.io/platform-charts
              version: "{{ .Values.system_roles.chartVersion }}"
        {{- end }}
        {{- if .Values.gatekeeper.enabled }}
          - application: "{{ $prefix }}gatekeeper-service"
            namespace: gatekeeper
            chart: 
              name: gatekeeper
              url: https://na-launch-workshop.github.io/platform-charts
              version: "{{ .Values.gatekeeper.chartVersion }}"
        {{- end }}
        # Probably Should go on shared w/ another chart for bootstrapping workers
        # Ill refactor when I get around to using it again
        {{- if .Values.acs.enabled }}
          - application: "{{ $prefix }}advanced-cluster-security-service"
            namespace: rhacs-operator
            chart: 
              name: acs
              url: https://na-launch-workshop.github.io/platform-charts
              version: "{{ .Values.acs.chartVersion }}"
        {{- end }}
        {{- if (and .Values.authorino.enabled (not .Values.rhoai.enabled)) }}
          - application: "{{ $prefix }}authorino-service"
            namespace: authorino
            chart: 
              name: authorino
              url: https://na-launch-workshop.github.io/platform-charts
              version: "{{ .Values.authorino.chartVersion }}"
        {{- end }}
        # Probably Should go on shared w/ another chart for bootstrapping workers
        # Ill refactor when I get around to using it again
        {{- if .Values.rhtas.enabled }}
          - application: "{{ $prefix }}rhtas-service"
            namespace: rhtas
            chart: 
              name: rhtas
              url: https://na-launch-workshop.github.io/platform-charts
              version: "{{ .Values.rhtas.chartVersion }}"
            values: |
              domain: {{ .Values.cluster.domain }}
        {{- end }}
        {{- if .Values.keycloak.enabled }}
          - application: keycloak-service
            namespace: keycloak
            chart: 
              name: keycloak
              url: https://na-launch-workshop.github.io/platform-charts
              version: "{{ .Values.keycloak.chartVersion }}"
            values: |
              domain: {{ .Values.cluster.domain }}
              shared:
                enabled: {{ .Values.shared.enabled }}
              cluster:
                name: {{ .Values.cluster.name }}
                region: {{ .Values.cloud.region }}
                type: {{ .Values.cluster.type }}
              settings:
                registrationAllowed: {{ .Values.settings.keycloak.registrationAllowed }}
        {{- end }}
  template:
    metadata:
      name: "{{ `{{ .application }}` }}"
    spec:
      destination:
        namespace: "{{ `{{ .namespace }}` }}"
        server: "https://kubernetes.default.svc"
      source:
        chart: "{{ `{{ .chart.name }}` }}"
        targetRevision: "{{ `{{ .chart.version }}` }}"
        repoURL: "{{ `{{ .chart.url }}` }}"
      project: system
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        managedNamespaceMetadata:
          labels:
            argocd.argoproj.io/managed-by: "{{ .Values.gitops.namespace }}"
        syncOptions:
          - CreateNamespace=true
          - Validate=false
  templatePatch: |
    spec:
      source:
        helm:
        {{ `{{- if (index . "values") }}` }}
          values: |
            {{ `{{- .values | nindent 12 }}` }}
        {{ `{{- end }}` }}
{{- end }}
